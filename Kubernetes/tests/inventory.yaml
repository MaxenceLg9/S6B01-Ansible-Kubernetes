all:
  vars:
    ansible_ssh_private_key_file: "../../SSH/sisyphe_key"
    ansible_user: etu # Or your specific user
    ansible_become: yes

  children:
    # K3s Cluster (Management/Rancher)
    k3s_cluster:
      children:
        rancher: # If this is a separate node for the Rancher UI
          hosts:
            192.168.122.3:

    # RKE2 Cluster (Workloads)
    rke2_cluster:
      children:
        workers:
          hosts:
            10.0.1.5:
            10.0.1.6:
        master:
          hosts:
            192.168.122.4: